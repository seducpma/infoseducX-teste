
<div id="interno6">
<% title "FUNCIONARIOS".singularize %>
<div class="tabs">
 <div id="caixa_titulo">
     <font size="3"><strong>CADASTRO DE FUNCIONARIOS</strong></font><br/>
 </div>
      <div class="tabs">
    <%  if current_user.unidade_id == 52 or current_user.unidade_id == 53 %>
       <% @unidades= Unidade.find(:all, :order => 'nome ASC') %>
     <%  else %>
        <% @unidades= Unidade.find(:all , :conditions => ['id = ?', current_user.unidade_id]) %>
      <% end%>
     <%= render :partial => 'form' %>
    </div>
</div>
<br/>
</div>

